import { EventsDToUrlPipe } from './events-d-to-url.pipe';

describe('EventsDToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsDToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
